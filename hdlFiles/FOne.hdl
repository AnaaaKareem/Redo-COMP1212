// File name: FOne.hdl
/**
 * FOne gate.
 *
 * Inputs: A, B, C, D
 * Outputs: F, G
 *
 * Inputs used: A, B, C, D
 *
 * Logic Circuit:
 * AC -> F
 * BD -> G
 */

CHIP FOne {
    IN A, B, C, D;
    OUT F, G;

    PARTS:

    // Output for F gate
    And(a=A, b=C, out=F);

    // Output for G gate
    And(a=B, b=D, out=G);
}