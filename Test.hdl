CHIP Test {

    IN  f0, f1, A, B, C, D, load;
    OUT E, F, G;

    PARTS:
    Mux(a=gayout, b=Fin, sel=load, out=gayin);
    DFF(in=gayin, out=f, out=gayout);
    Mux(a=f, b=C, sel=load, out=c);

    FALL(f0=f0, f1=f1, A=A, B=B, C=c, D=D, E=E, F=Fin, G=Gin);
    Or(a=Fin, b=Fin, out=F);
}